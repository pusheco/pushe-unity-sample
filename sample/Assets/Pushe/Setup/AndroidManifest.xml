<?xml version="1.0" encoding="utf-8"?>

<!-- AndroidManifest sample. If you don't have one, Copy this to `Assets/Plugins/Android` and add the token -->

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="preferExternal">
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"/>

        <!-- In order to receive the callback you must add `android:name="co.pushe.plus.ext.PusheUnityApplication"` to <application /> attributes -->
        <!-- If you use MultiDex, for getting the callbacks make it use `android:name="co.pushe.plus.ext.PusheMultiDexApplication"` to apply multiDex -->
        <!-- In case none of these two were possible, In your own native application onCreate() code, call: `co.pushe.plus.ext.PusheExt.initializeNotificationListener` -->

    <application
        android:name="co.pushe.plus.ext.PusheMultiDexApplication"
        android:theme="@style/UnityThemeSelector"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name">
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>

        <meta-data android:name="pushe_token" android:value="PLUS_TOKEN" />
        
    </application>
</manifest>
